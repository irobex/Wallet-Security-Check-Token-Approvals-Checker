Проект: Wallet Guard — Wallet Approvals & Risk Check (USDT TRC20 Payments)
0) Цель продукта

Создать систему, которая:

Органически находится через Google по запросам про безопасность кошелька (SEO через GitHub README).

Дает бесплатное превью (доверие и “вау”).

Продаёт платный отчёт в Telegram-боте (3 тарифа).

Принимает оплату USDT-TRC20 по схеме уникальный адрес на заказ.

Автоматически проверяет оплату и отправляет пользователю результат (без участия оператора).

1) Пользовательский флоу (end-to-end)
1.1 GitHub → Telegram (органика)

Пользователь ищет в Google: “wallet approvals check”, “revoke token approvals”, “wallet security check”, “unlimited approval risk”, “как проверить разрешения токенов”, “отозвать апрувы”, “кошелек безопасность approve”.

Попадает в GitHub репозиторий.

В README видит:

что инструмент read-only,

пример отчёта,

кнопку/ссылку на Telegram-бота,

бесплатный mini-script (просмотр approvals без рекомендаций),

описание тарифов в боте.

1.2 Telegram-бот: покупка и доставка

/start → бот объясняет: “не просим seed / не подключаем кошелёк / только публичный адрес”.

Кнопка “🔍 Проверить кошелёк”.

Пользователь вводит адрес 0x....

Бот делает бесплатное превью:

число активных approvals,

количество unlimited approvals,

топ-3 токена по approvals,

предложение выбрать тариф.

Пользователь выбирает тариф Lite / Pro / Max.

Бот создаёт заказ и выдаёт:

сумму в USDT,

уникальный TRON-адрес оплаты,

инструкцию “отправьте USDT TRC20 на этот адрес”,

статус “Ожидаю оплату”.

Сервис автоматически мониторит блокчейн TRON и находит поступление USDT на адрес.

После подтверждения:

бот присылает отчёт,

PDF/HTML файл (по тарифу),

кнопки: “Открыть revoke-ссылки”, “Показать HIGH”, “Экспорт CSV”.

2) Тарифы (3 уровня)
2.1 Lite (дешёвый, “пощупать”)

Цена: 1 USDT *(temporary for e2e payment testing)*
Функции:

1 сеть анализа: Ethereum

Сводка:

approvals total

unlimited approvals count

Ограничение: максимум 30 approvals в отчёте (первые по “опасности”)

Без “подозрительных контрактов” и без расширенных рекомендаций

Доставка: текст + CSV (без PDF)

2.2 Pro (основной продающий)

Цена: 25 USDT
Функции:

Ethereum (обязательно)

Полный список approvals без лимита (в разумных пределах: например до 500 строк)

Риск-оценка по правилам:

unlimited allowance (HIGH)

approval старше 180 дней и не использовался (MEDIUM)

spender = контракт без верификации/без имени (MEDIUM)

Список “REVOKE NOW / REVIEW / OK”

Доставка: текст + CSV + HTML отчёт

2.3 Max (дороже, “спокойствие”)

Цена: 79 USDT
Функции:

Всё из Pro

“Interaction snapshot” за 90 дней (если возможно по источникам данных):

последние взаимодействия с новыми контрактами

частые spender’ы

Мониторинг 30 дней:

бот шлёт уведомление при появлении нового unlimited approval

и при появлении нового spender

Доставка: текст + CSV + PDF отчёт

Примечание: мониторинг реализовать через ежедневный cron/worker: перескан approvals и сравнивать с последним сохранённым снапшотом.

3) Архитектура (техническая)
3.1 Стек

Язык/рантайм: Node.js 20 + TypeScript

Telegram: grammY (или telegraf, но выбрать один)

Web3: ethers (Ethereum)

TRON: tronweb

DB: PostgreSQL 16

PDF/HTML:

HTML: шаблоны ejs

PDF: playwright (рендер HTML в PDF) или puppeteer (выбрать один)

Инфраструктура: Docker Compose

3.2 Компоненты

Bot Service — обработка сообщений Telegram, создание заказов, выдача реквизитов, отправка результатов.

Payments Worker — мониторинг TRC20 оплат, подтверждение заказов.

Reports Worker — генерация отчётов (HTML/PDF/CSV), сохранение в БД/файлы, отправка пользователю.

Scheduler — мониторинг Max-тарифа (ежедневные проверки approvals).

4) TRC20 оплата (схема 2 — уникальный адрес на заказ)
4.1 Требования

Для каждого заказа генерировать уникальный TRON-адрес (по HD-кошельку).

Поддерживать только USDT TRC20.

Заказ считается оплаченным, если:

найден TRC20 Transfer на адрес заказа

сумма >= ожидаемой (точно равна или больше — на усмотрение, но лучше “>=”, чтобы не блокировать)

подтверждения: минимум 1 (TRON быстрый)

4.2 HD-кошелёк TRON

Использовать mnemonic (BIP39) и derivation path TRON: m/44'/195'/0'/0/i

Хранить:

TRON_MNEMONIC (env)

TRON_HD_INDEX (в DB, автоинкремент)

При создании заказа:

взять следующий индекс i

сгенерировать адрес

сохранить в orders.pay_address и orders.hd_index

4.3 Проверка оплат

Реализовать worker:

Период: каждые 15 секунд

Берём все orders со статусом PENDING_PAYMENT

Для каждого заказа:

проверяем входящие TRC20 переводы USDT на pay_address через TronGrid API или через tronweb запросы.

если найдено:

сохраняем tx_hash, paid_amount, paid_at

переводим статус в PAID

ставим задачу на генерацию отчёта

USDT TRC20 contract address (mainnet):
TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t

5) Отчёт по безопасности (что считать “ценностью”)
5.1 Что такое “approval” в контексте отчёта

Для ERC20 токенов:

allowance владельца (wallet) к spender (контракт)

если allowance огромный (например 2^256-1 или > 1e30) — считать unlimited

5.2 Алгоритм получения approvals (Ethereum)

Есть два способа, выбираем один для MVP:

MVP (быстро и проще): использовать внешний индексатор API (например Etherscan токен approvals не всегда, но можно собрать через события Approval + состояния allowance).
Правильнее для продакшена:

собрать список токенов, с которыми wallet взаимодействовал (по транзакциям/ERC20 transfers)

для каждого токена вызвать allowance(owner, spender) для spender’ов, которые встречались в Approval событиях

но это может быть тяжело без индексатора

Для MVP допускается использование провайдера данных (например Covalent/Alchemy/Blockscout/DeBank-подобные API) — но реализация должна быть модульной, чтобы потом заменить.

5.3 Правила риск-скоринга (конкретно)

HIGH:

unlimited allowance на популярные стейблы (USDT/USDC/DAI) и spender неизвестен

unlimited allowance и approval был создан недавно (последние 30 дней) и spender не из allowlist

MEDIUM:

allowance большой (выше порога) и approval старше 180 дней

spender не верифицирован/нет метаданных

LOW:

малые allowances, известные spender’ы (Uniswap, Aave и т.д. — allowlist)

5.4 “Actionable” часть

Отчёт должен выдавать:

“Сначала отзови это” (top 5 HIGH)

“Проверить” (MEDIUM)

“Можно оставить” (LOW)

Для каждого HIGH/MEDIUM дать:

token symbol

spender address

allowance (читабельно)

причина риска (строка)

“revoke link”

6) Revoke links (куда ведут кнопки)

Сделать ссылки на популярные revoke-инструменты:

revoke.cash (основной)

альтернативы (добавить одну запасную)

Формат ссылок:

Базовая ссылка на сеть + адрес (если сервис поддерживает prefill — использовать; если нет, просто общая ссылка + подсказка)

7) Telegram UX (точные экраны и тексты)
7.1 /start

Текст (встроить в код как константу):

“Wallet Guard проверяет approvals и риски. Read-only. Мы НЕ просим seed phrase. Мы НЕ просим подключать кошелёк. Введите публичный адрес.”
Кнопки:

“🔍 Проверить кошелёк”

“💳 Тарифы”

“❓ Как это работает”

7.2 Ввод адреса

Валидация: 0x + 40 hex символов

Если неверно: “Это не похоже на Ethereum-адрес. Пришлите адрес вида 0x…”

7.3 Free preview

Показывать:

approvals total

unlimited count

топ токенов
И кнопки:

“🟢 Lite — 1 USDT”

“🟡 Pro — 25 USDT”

“🔴 Max — 79 USDT”

7.4 Экран оплаты

“Оплатите USDT (TRC20) на адрес: …”

“Сумма: … USDT”

“После оплаты отчёт придёт автоматически (обычно до 1 минуты).”
Кнопки:

“🔄 Проверить оплату” (ручной триггер, но реальную проверку делает worker)

“⬅️ Назад”

7.5 Доставка результата

Сообщение summary + файл

Кнопки:

“📌 Показать HIGH”

“📄 Скачать CSV”

“🔗 Revoke links”

8) База данных (PostgreSQL) — конкретные таблицы
8.1 users

id UUID PK

telegram_id BIGINT UNIQUE NOT NULL

created_at TIMESTAMP NOT NULL DEFAULT now()

8.2 orders

id UUID PK

user_id UUID FK users(id)

wallet_address VARCHAR(42) NOT NULL

plan VARCHAR(10) NOT NULL CHECK IN ('LITE','PRO','MAX')

price_usdt NUMERIC(10,2) NOT NULL

status VARCHAR(20) NOT NULL CHECK IN ('CREATED','PENDING_PAYMENT','PAID','REPORTING','DELIVERED','EXPIRED','FAILED')

pay_address VARCHAR(64) NOT NULL

hd_index INTEGER NOT NULL

tx_hash VARCHAR(128)

paid_amount NUMERIC(18,6)

created_at TIMESTAMP NOT NULL DEFAULT now()

paid_at TIMESTAMP

delivered_at TIMESTAMP

Индексы:

orders(status)

orders(pay_address)

orders(user_id, created_at desc)

8.3 reports

id UUID PK

order_id UUID FK orders(id) UNIQUE

risk_level VARCHAR(10) CHECK IN ('LOW','MEDIUM','HIGH')

summary_text TEXT NOT NULL

csv_path TEXT

html_path TEXT

pdf_path TEXT

data_json JSONB NOT NULL

created_at TIMESTAMP NOT NULL DEFAULT now()

8.4 monitoring_subscriptions (для MAX)

id UUID PK

user_id UUID FK

wallet_address VARCHAR(42)

active BOOLEAN DEFAULT true

expires_at TIMESTAMP NOT NULL

last_snapshot_json JSONB

created_at TIMESTAMP DEFAULT now()

9) Репозиторий и структура файлов (один монорепо)
wallet-guard/
  README.md
  LICENSE
  docker-compose.yml
  .env.example
  package.json
  tsconfig.json
  src/
    bot/
      bot.ts
      handlers/
        start.ts
        checkWallet.ts
        plans.ts
        pay.ts
        help.ts
      ui/
        texts.ts
        keyboards.ts
    core/
      validation.ts
      config.ts
      logger.ts
    db/
      index.ts
      migrations/
        001_init.sql
    payments/
      tron/
        hd.ts
        usdt_trc20.ts
        monitor.ts
    reports/
      generator.ts
      scoring.ts
      revokeLinks.ts
      templates/
        report.ejs
    workers/
      paymentsWorker.ts
      reportsWorker.ts
      monitoringWorker.ts
  scripts/
    free-approvals-viewer/
      index.ts
      package.json
      README.md

10) .env (конкретные переменные)

.env.example должен содержать:

BOT_TOKEN=

DATABASE_URL=postgresql://walletguard:walletguard@db:5432/walletguard

TRON_MNEMONIC=

TRONGRID_API_KEY=

ETH_RPC_URL=

REPORTS_STORAGE_PATH=/data/reports

ADMIN_TELEGRAM_ID=123456789 (для алертов об ошибках)

11) GitHub SEO (обязательная часть)
11.1 Название репозитория

Рекомендованное имя (SEO):
wallet-security-check-approvals-revoke

11.2 README.md — требования к SEO-тексту

README должен содержать:

H1 с ключами:

“Wallet Security Check”

“Token Approvals Checker”

“Revoke Unlimited Approvals”

Подзаголовки H2/H3 с ключами (англ + рус):

“Check token approvals”

“How to revoke token approvals”

“Unlimited approval risk”

“Wallet security scan”

“Проверка апрувов”

“Как отозвать разрешения токенов”

FAQ с прямыми вопросами (как люди ищут):

“Is it safe? Do you need my seed phrase?”

“How to check approvals on Ethereum?”

“How to revoke approvals safely?”

“What is unlimited approval?”

Вставить блок “Telegram bot” с CTA:

“Get full report in Telegram in 1 minute (USDT TRC20)”

Добавить “Keywords” секцию (plain text) со списком 30–50 ключевых фраз.

11.3 Контент, который повышает конверсию

Скриншот примера отчёта (текстовый пример + картинка)

“Free preview vs Pro report” таблица

Чёткое обещание: read-only, без подключения кошелька, без seed

12) Бесплатный скрипт на GitHub (чтобы “доказывал”)

В папке scripts/free-approvals-viewer сделать мини-утилиту:

команда:

node index.js 0x...

вывод:

количество approvals (если доступно)

список spender адресов

подсказка “Полный отчёт в Telegram”

Этот скрипт должен быть максимально простым, чтобы:

работал на Windows/Mac

запускался одной командой после npm i

13) Нефункциональные требования (качество)

Никаких запросов seed phrase, private key, подключения кошелька.

Логи не должны содержать mnemonic, токены, приватные данные.

Все ошибки worker’ов отправлять администратору в Telegram (по ADMIN_TELEGRAM_ID) кратким сообщением.

Заказы истекают через 60 минут: EXPIRED.

Если пришла оплата на истёкший заказ — пометить PAID и всё равно доставить (или создать правило “refund manual” — но для MVP лучше доставлять).

14) Acceptance Criteria (критерии готовности)

Система считается готовой, если:

Пользователь из Telegram может:

получить бесплатное превью,

выбрать тариф,

получить уникальный TRON-адрес,

оплатить USDT TRC20,

получить отчёт автоматически.

Payment Worker:

корректно находит TRC20 Transfer на pay_address,

переводит заказ в PAID,

не путает оплаты между заказами.

README на GitHub:

содержит ключевые SEO-разделы,

содержит CTA на бота,

содержит free script и инструкцию.

Режим разработки (как делать в Cursor)

Сначала реализовать сквозной happy-path: бот → заказ → адрес → фиктивная “оплата” через заглушку → отчёт.

Потом подключить реальный TRON мониторинг.

Потом генерацию HTML/PDF.

Потом тарифы Lite/Max и мониторинг.