# План тестирования (RU) — Wallet Guard

Этот документ описывает **набор проверок/кейсов** (от юнит-тестов до e2e “бот → оплата → отчёт”).

Важно: исходно он писался “раньше кода”, поэтому **рабочий документ для исполнения** теперь:
- `docs/05_TEST_ROADMAP_RU.md` (там чеклист + отметки прогонов).

---

## 1) Цели тестирования

- Проверить корректность логики approvals/allowance и риск-скоринга.
- Проверить корректность схемы оплат USDT TRC20 по “уникальному адресу на заказ”.
- Проверить надёжность воркеров (идемпотентность, дедуп, восстановление после рестартов).
- Проверить, что бот **read-only**, не запрашивает seed/private key и не требует подписей.
- Проверить, что артефакты отчёта (CSV/HTML/PDF) формируются и доставляются в Telegram.

---

## 2) Уровни тестов

### 2.1 Юнит-тесты (быстрые, изолированные)

**Цель**: проверить “чистую” бизнес-логику без сети и без БД.

- **Approvals / scoring**
  - `isUnlimited()`:
    - MaxUint256 → true
    - “огромное” значение (выше порога по decimals) → true
    - нормальные значения → false
  - Risk rules:
    - unlimited + UNKNOWN + major token → HIGH
    - unlimited + KNOWN_SAFE → MEDIUM
    - spender EOA + allowance > 0 → MEDIUM
    - old approval (>=180d) + UNKNOWN → MEDIUM
  - Action generation:
    - HIGH → REVOKE_NOW
    - MEDIUM → REVIEW
    - LOW → OK
  - Сортировка urgent-списка (major+HIGH наверх).
- **Revoke links**
  - `buildRevokeLink()` возвращает ожидаемый URL/формат (по chainId).
- **CSV**
  - Генератор CSV: корректные колонки, корректные значения, корректные разделители.
- **Валидация**
  - ETH адрес: принимает `0x` + 40 hex, отклоняет всё остальное.

### 2.2 Интеграционные тесты (с БД и/или с моками провайдеров)

**Цель**: проверить “связки” компонентов.

- **DB**
  - миграции применяются на чистую БД
  - вставка/обновление orders/reports работает
  - индексы/ограничения не мешают ожидаемым сценариям
- **TRON**
  - HD-генерация адресов стабильна (один mnemonic+index → один адрес)
  - выделение следующего `hd_index` атомарно (нет дублей при параллельных заказах)
  - payments worker:
    - находит оплату по API (в тестах — через мок TronGrid/TronWeb)
    - не повторяет обработку одного tx (дедуп)
- **Ethereum**
  - ApprovalEventProvider (в тестах — мок) выдаёт пары (token, spender)
  - allowance snapshot фильтрует нули
  - token metadata cache корректно обрабатывает reverts/таймауты

### 2.3 E2E тест (сквозной сценарий)

**Цель**: повторить пользовательский happy-path максимально близко к “прод”.

Сценарий:
- `/start` → бот показывает описание read-only и кнопки
- “🔍 Проверить кошелёк” → ввод валидного `0x...`
- free preview показывается и содержит:
  - approvals total
  - unlimited count
  - top tokens
- выбор тарифа → создаётся order со статусом `PENDING_PAYMENT`, показывается TRON-адрес
- имитируем оплату:
  - либо реальной тестовой транзакцией USDT TRC20 (если есть тестовая среда)
  - либо мок/заглушка провайдера TRON в dev-режиме
- payments worker переводит order в `PAID`
- reports worker генерирует отчёт и отправляет:
  - summary текст
  - CSV (всегда)
  - HTML/PDF по тарифу
- проверка кнопок бота:
  - “Показать HIGH”
  - “Скачать CSV”
  - “Revoke links”

---

## 3) Наборы тест-данных

- **ETH адреса**
  - “пустой” адрес без approvals (ожидаем 0 / чистый отчёт)
  - адрес с несколькими approvals (ожидаем список)
  - адрес с unlimited approvals (ожидаем HIGH)
- **TRON**
  - 2–3 заказа подряд (проверка, что адреса разные, hd_index растёт)
  - повторная “видимость” одного и того же tx (идемпотентность)

---

## 4) Негативные сценарии (обязательно)

- Неверный ETH адрес → корректное сообщение об ошибке.
- RPC/провайдер Ethereum недоступен → бот/воркер не падает, пользователю/админу понятное сообщение.
- TronGrid недоступен → payments worker ретраит, не ломает статусы.
- Оплата меньше суммы → заказ остаётся PENDING (или отдельный статус/обработка — по принятому правилу).
- Оплата после `EXPIRED` → заказ помечается `PAID` и отчёт всё равно доставляется (MVP правило из ТЗ).
- Повторная обработка заказа (рестарт воркера) → нет дублирования отчётов/сообщений.

---

## 5) Проверки безопасности/комплаенса

- Бот нигде не просит seed/private key/подпись.
- Логи не содержат `TRON_MNEMONIC`, токены, секреты.
- `.env` не коммитится, существует `.env.example`.

---

## 6) Производительность и лимиты

- Проверить лимиты:
  - max unique token contracts (например 300)
  - max (token, spender) pairs (например 2000)
  - graceful degradation + предупреждение в отчёте
- Замер времени типичных прогонов:
  - Lite: целевой <= 20s
  - Pro: <= 60s
  - Max: <= 90s

---

## 7) Acceptance checklist (готовность MVP)

- [ ] Telegram happy-path работает: адрес → превью → заказ → “оплата” → отчёт.
- [ ] Payments worker находит перевод USDT TRC20 и не путает заказы.
- [ ] Отчёт содержит risk level + причины + actions + revoke links.
- [ ] CSV генерируется и скачивается.
- [ ] Ошибки воркеров отправляются админу в Telegram.
- [ ] README соответствует SEO требованиям и содержит инструкцию free script.



